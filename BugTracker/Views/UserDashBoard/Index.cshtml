@model BugTracker.Models.UserDashBoardViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Dash Board Index</h2>
@if (HttpContext.Current.User.IsInRole("Submitter"))
{
    <p>
        @Html.ActionLink("Create New Ticket", "Create", "Tickets", null, null)
    </p>
}

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@

<div class="row">
    <div class="col-md-2">@*The Left Board*@

    </div>

    <div class="col-md-2">@*The tickets Board*@
    @{
        Html.RenderPartial("TicketsIndexPartial", (object)ViewBag.TicketsModel);
    }
        
    </div>


    <div class="col-md-2">@*The rigth Board*@

    </div>
</div>

@*@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}*@

